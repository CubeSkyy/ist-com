public void printd(number n)
public number factorial(integer i)
public integer atoi(string s)


public integer entry (integer argc, string *argv, string *envp) {
  integer n;

  if argc = 1 then n := 5;
  else if argc = 2 then n := atoi(argv[1]);
  else entry := 1;

  printd(factorial(n));

  entry := 0
};
